# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# runtime settings store (server-side; holds UI-entered API keys — NEVER commit)
/.data

# env — NEVER commit real secrets; .env.example is committed
.env
.env*.local
.env.local
.env.development
.env.production

# drizzle
/drizzle

# transformers.js / local model cache
/.cache
/models

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
.idea
*.tsbuildinfo
